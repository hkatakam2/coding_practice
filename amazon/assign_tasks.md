### question
Given arrays of task difficulties and corresponding profits plus worker abilities, assign at most one task to each worker (a worker can do any task with difficulty ≤ their ability) — tasks may be reused by multiple workers — to maximize the total profit.

